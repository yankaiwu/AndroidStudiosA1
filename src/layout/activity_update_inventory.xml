<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/frameHorizontal"
    android:paddingRight="@dimen/frameHorizontal"
    android:paddingTop="@dimen/frameVertical"
    tools:context=".UpdateInventoryActivity">

    <TextView
        android:id="@+id/updateInventoryHeading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="@string/updateInventoryHeading"
        android:textSize="@dimen/headingText"/>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="350sp"
        android:layout_below="@+id/updateInventoryHeading"
        android:layout_marginTop="20sp">
        <include layout="@layout/inventory_container"
            android:id="@+id/inventory_view"/>
    </ScrollView>


</RelativeLayout>